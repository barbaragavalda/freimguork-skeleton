# freimguork-skeleton

Plantilla base para arrancar un proyecto nuevo que depende de `freimguork-core`
(y opcionalmente `freimguork-appacman`).

Contenido pendiente de definir: estructura de `config/`, `public/`, `src/`,
`composer.json` y, si aplica, un `.sql` inicial.
